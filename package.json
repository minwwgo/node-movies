{
  "name": "node-movies",
  "version": "1.0.0",
  "description": "learning node api server from basic",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"

  },
  "author": "min",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
